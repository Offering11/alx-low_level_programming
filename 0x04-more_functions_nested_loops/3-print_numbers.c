#include "main.h"

/**
* print_numbers - is a function that check for uppercase character
*
* Return : Successful will return 0
*/

void print_numbers(void)
{
	int a;

	for (a = 0; a <= 9; a++)
		_putchar(a);
	_putchar('/n');
	return (0);
}
